{
  "name": "now-next-example-builder-app",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "standard",
    "lint": "standard",
    "lint:watch": "nodemon --quiet --exec 'standard && echo linting: OK' --watch '**/*.js' --ignore node_modules"
  },
  "dependencies": {
    "next": "^8.0.3",
    "now-next-example-builder-fixture": "file:..",
    "react": "^16.8.4",
    "react-dom": "^16.8.4"
  },
  "devDependencies": {
    "nodemon": "^1.18.10",
    "standard": "^12.0.1"
  }
}
